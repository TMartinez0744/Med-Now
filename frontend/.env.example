# URL del backend. En local dejar en blanco (cae a http://localhost:3000).
# En producción (Vercel): la URL del backend deployado en Render/Railway.
VITE_API_URL=

# Supabase (mismo proyecto principal que usa el backend)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
